{
  "name": "worldcup2026-app",
  "version": "1.0.0",
  "private": true,

  "scripts": {
    "start": "npx serve .",
    "build": "echo build complete",
    "android": "npx cap open android"
  },

  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },

  "devDependencies": {
    "@capacitor/cli": "^7.0.0",
    "serve": "^14.2.1"
  }
}